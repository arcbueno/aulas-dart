void main() {
  var nomesCadastrados = ['Ingrid', 'Ricardo', 'Pedro'];
  // print(nomesCadastrados[0]);
  print(nomesCadastrados.first);
  print(nomesCadastrados.last);
  nomesCadastrados.add('Rebeca');
  print(nomesCadastrados.last);
}
